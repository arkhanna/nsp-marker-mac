[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "nsp-marker"
version = "0.1.0"
description = "Send timestamped comment markers to a Blackrock NSP over UDP"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = ["nsp_marker"]
